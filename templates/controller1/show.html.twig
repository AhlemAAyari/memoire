{% extends 'base.html.twig' %}
{% block body %} 
<article> <h2>{{article.fullname}}
 </h2> <div class="metadata">Edited at{{article.createdAt | date("m/d/Y")}}
  à {{article.createdAt | date("h/i")}}
  </div> <div class="content"> Urgency:
  {% if article.urgency==1 %}
  Yes<br>
  {% else %}
  No<br>
  {% endif %}
   {{article.disease | raw }} </div> </article>{% endblock %}